# main.py
def generate_document(data):
    """A simple function to simulate document generation."""
    print(f"Generating document using data: {data}")

if __name__ == "__main__":
    generate_document("Initial setup complete.")
